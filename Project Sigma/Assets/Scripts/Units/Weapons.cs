using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class Weapons
{
    /*
     * While there may eventually be a huge array of different weapons,
     * for the sake of testing we will be starting with the following weapons:
     *  Bronze Sword
     *  Iron Sword
     *  Bow
     *  Fire Ring    
    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
